[buildout]
extends =
	base.cfg
	sources.cfg
	versions.cfg
show-picked-versions = true
update-versions-file = profiles/versions.cfg

[scripts]
eggs +=
    nose
    WebTest
    coverage
    mock
    repoze.profile
scripts +=
    pserve
    nosetests

[circus.ini]
webapp =

[openprocurement.api.ini]
debug = true

