{
   "_id": "_design/backup",
   "language": "javascript",
   "validate_doc_update": "function(newDoc, oldDoc, userCtx, secObj) {\n            if(newDoc._id.indexOf('_design/') === 0) {\n                throw({forbidden: 'Skip _design document'});\n            }\n            return true;\n        }\n        "
}
